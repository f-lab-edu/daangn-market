<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.limikju.daangn_market.repository.mybatis.CategoryRepository">
  <select id="hasChild">
    SELECT EXISTS (
    SELECT 1
    FROM CATEGORY
    WHERE parent_id = #{id}
    );
  </select>

  <select id="findByTitle" resultType="com.limikju.daangn_market.domain.Category">
    SELECT * FROM CATEGORY WHERE title = #{title}
  </select>
</mapper>